<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="hoperun.pagoda.demo.mapper.GroupMapper">
     <select id="findAllGroup" resultType="hoperun.pagoda.demo.entity.Group">
        SELECT * from tb_group;
    </select>
     <select id="findGroupNameById" resultType="String">
        SELECT groupname from tb_group where group_id =#{groupId};
    </select>
    <insert id="insert" parameterType="hoperun.pagoda.demo.entity.Group" useGeneratedKeys="true" keyProperty="group_id">
        insert into tb_group (groupname, customer_id,notes_server,notes_server_user,notes_server_pasword,description) VALUES 
        (#{groupname}, #{customer_id}, #{notes_server}, #{notes_server_user}, #{notes_server_pasword}, #{description});
    </insert>
    
    <select id="findCustomersByGroupId" resultType="int">
        SELECT customer_id from tb_group where group_id =#{groupId};
    </select>
    
   <update id="updateGroupCustomer" parameterType="java.util.List">
        update tb_group set customer_id=null where 1=2
        <if test="customers!=null and customers.size()>0">
            or customer_id in
            <foreach collection="customers" item="customer_id" index="index"
                open="(" close=")" separator=",">
                 #{customer_id}
            </foreach>
        </if>
   </update>
</mapper>